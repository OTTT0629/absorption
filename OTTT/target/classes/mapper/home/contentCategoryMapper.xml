<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.ottt.ottt.dao.home.ContentCategoryMapper">
  
  <select id="categorySelect" parameterType="int" resultType="ContentCategoryDTO">
  	SELECT c.content_no, c.category_no, tc.category_nm
	FROM tb_content_category c, tb_category tc 
	where c.content_no = tc.category_no and content_no = #{content_no}
  </select>
  
  </mapper>