<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.ottt.ottt.controller.ContentMapper">
  
  <select id="ratingSelect" resultType="ContentDTO">
  	SELECT *
	FROM tb_content
	order by rating desc 
	limit 14
  </select>
  
  <select id="contentGenreName" parameterType="int" resultType="ContentDTO">
	SELECT tg.genre_nm
	FROM tb_content_genre tcg, tb_content c, tb_genre tg 
	where c.content_no = tcg.content_no  and tg.genre_no = tcg.genre_no 
	and c.content_no = #{content_no}
	group by tg.genre_nm
  </select>
  
  </mapper>